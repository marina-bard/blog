{% extends 'base.html.twig' %}

{% block body %}

    <p class="blog-subtitle"> Post edit
      <a href="{{ path('_index') }}">Back to the list</a>
    </p>

    <div class="blog-post">
      <p>
        {{ form_start(edit_form) }}
          {{ form_widget(edit_form) }}
            <input class="btn btn-primary pull-right" type="submit" value="Edit">
        {{ form_end(edit_form) }}
        {{ form_start(delete_form) }}
          <input class="btn btn-primary pull-right" type="submit" value="Delete">
        {{ form_end(delete_form) }}
      </p>
    </div>

{% endblock %}
